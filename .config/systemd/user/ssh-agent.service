[Unit]
Description=SSH key agent
Documentation=man:ssh-agent(1)
Conflicts=ssh-agent.socket
Before=sockets.target

[Service]
Type=simple
ExecStart=/usr/bin/ssh-agent -D -a %t/ssh-agent.socket

[Install]
WantedBy=default.target
