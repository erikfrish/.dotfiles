binds {
    // Show Niri's built-in cheat sheet for the bindings below.
    Mod+Shift+Slash { show-hotkey-overlay; }

    // Layout toggle. Using a Niri keybind instead of XKB grp:win_space_toggle
    // so the space keypress is consumed and XWayland apps properly receive the
    // layout change notification.
    Mod+Space { switch-layout "next"; }

    // App launchers. Keep these aligned with Hyprland defaults where possible.
    Mod+I hotkey-overlay-title="Open VSCode" { spawn "code"; }
    Mod+O hotkey-overlay-title="Open Overview" { toggle-overview; }
    Mod+T hotkey-overlay-title="Open Terminal" { spawn "alacritty"; }
    Mod+E hotkey-overlay-title="Open Files" { spawn "nautilus"; }
    Mod+R hotkey-overlay-title="Open Menu" { spawn-sh "~/.config/desktop/scripts/menu"; }

    // Window state and column management.
    Mod+Q { close-window; }
    Mod+V { toggle-window-floating; }
    Mod+Shift+V { switch-focus-between-floating-and-tiling; }
    Mod+F { fullscreen-window; }
    Mod+Ctrl+F { maximize-column; }
    Mod+BracketRight { consume-or-expel-window-right; }
    Mod+BracketLeft { consume-or-expel-window-left; }

    // Small desktop utilities.
    Mod+P { spawn "hyprpicker" "-a"; }
    Mod+W { spawn-sh "~/.config/waybar/scripts/waybar_style_change"; }
    Mod+B { spawn-sh "killall waybar || waybar &"; }
    Mod+C { spawn-sh "~/.config/desktop/scripts/clipboard_history"; }

    // Hardware keys. Audio goes through the shared volume script so both Niri
    // and Hyprland show the same wob OSD and avoid hardcoded sink names.
    XF86AudioRaiseVolume allow-when-locked=true { spawn-sh "~/.config/desktop/scripts/volume --inc"; }
    XF86AudioLowerVolume allow-when-locked=true { spawn-sh "~/.config/desktop/scripts/volume --dec"; }
    XF86AudioMute allow-when-locked=true { spawn-sh "~/.config/desktop/scripts/volume --toggle"; }
    XF86AudioMicMute allow-when-locked=true { spawn-sh "~/.config/desktop/scripts/volume --toggle-mic"; }

    // Brightness writes directly to the Niri wob FIFO.
    XF86MonBrightnessUp allow-when-locked=true { spawn-sh "brightnessctl -e4 -n2 set 5%+ | sed -En 's/.*\\( ([0-9]+)%\\).*/\\1/p' > /tmp/niri.wob"; }
    XF86MonBrightnessDown allow-when-locked=true { spawn-sh "brightnessctl -e4 -n2 set 5%- | sed -En 's/.*\\( ([0-9]+)%\\).*/\\1/p' > /tmp/niri.wob"; }

    XF86AudioPlay allow-when-locked=true { spawn-sh "playerctl play-pause"; }
    XF86AudioPause allow-when-locked=true { spawn-sh "playerctl play-pause"; }
    XF86AudioNext allow-when-locked=true { spawn-sh "playerctl next"; }
    XF86AudioPrev allow-when-locked=true { spawn-sh "playerctl previous"; }

    // Niri is column-first: left/right move between columns, up/down between
    // workspaces. This matches Hyprland's workspace-nav muscle memory.
    // No modifier arrows: focus. Ctrl+arrows: move window.
    Mod+Left { focus-column-left; }
    Mod+Right { focus-column-right; }
    Mod+Up { focus-workspace-up; }
    Mod+Down { focus-workspace-down; }
    Mod+H { focus-column-left; }
    Mod+L { focus-column-right; }
    Mod+K { focus-workspace-up; }
    Mod+J { focus-workspace-down; }

    // Move focused window: Ctrl+arrows.
    // Left/Right moves column within workspace, Up/Down moves to another workspace.
    Mod+Ctrl+Left { move-column-left; }
    Mod+Ctrl+Right { move-column-right; }
    Mod+Ctrl+Up { move-window-to-workspace-up; }
    Mod+Ctrl+Down { move-window-to-workspace-down; }
    Mod+Ctrl+H { move-column-left; }
    Mod+Ctrl+L { move-column-right; }
    Mod+Ctrl+K { move-window-to-workspace-up; }
    Mod+Ctrl+J { move-window-to-workspace-down; }

    // Shift+arrows: in-column reordering (move window up/down within a column,
    // or column left/right — same as Ctrl but kept for habit).
    Mod+Shift+Left { move-column-left; }
    Mod+Shift+Right { move-column-right; }
    Mod+Shift+Up { move-window-up; }
    Mod+Shift+Down { move-window-down; }
    Mod+Shift+H { move-column-left; }
    Mod+Shift+L { move-column-right; }
    Mod+Shift+K { move-window-up; }
    Mod+Shift+J { move-window-down; }

    // Resize the current column/window.
    Mod+Ctrl+R { reset-window-height; }
    Mod+Minus { set-column-width "-10%"; }
    Mod+Equal { set-column-width "+10%"; }
    Mod+Shift+Minus { set-window-height "-10%"; }
    Mod+Shift+Equal { set-window-height "+10%"; }

    // Direct workspace access.
    Mod+1 { focus-workspace 1; }
    Mod+2 { focus-workspace 2; }
    Mod+3 { focus-workspace 3; }
    Mod+4 { focus-workspace 4; }
    Mod+5 { focus-workspace 5; }
    Mod+6 { focus-workspace 6; }
    Mod+7 { focus-workspace 7; }
    Mod+8 { focus-workspace 8; }
    Mod+9 { focus-workspace 9; }
    Mod+0 { focus-workspace 10; }

    // Move windows to workspaces. Ctrl follows focus; Shift mirrors Hyprland's
    // silent move style for muscle memory.
    Mod+Ctrl+1 { move-window-to-workspace 1; }
    Mod+Ctrl+2 { move-window-to-workspace 2; }
    Mod+Ctrl+3 { move-window-to-workspace 3; }
    Mod+Ctrl+4 { move-window-to-workspace 4; }
    Mod+Ctrl+5 { move-window-to-workspace 5; }
    Mod+Ctrl+6 { move-window-to-workspace 6; }
    Mod+Ctrl+7 { move-window-to-workspace 7; }
    Mod+Ctrl+8 { move-window-to-workspace 8; }
    Mod+Ctrl+9 { move-window-to-workspace 9; }
    Mod+Ctrl+0 { move-window-to-workspace 10; }

    Mod+Shift+1 { move-window-to-workspace 1; }
    Mod+Shift+2 { move-window-to-workspace 2; }
    Mod+Shift+3 { move-window-to-workspace 3; }
    Mod+Shift+4 { move-window-to-workspace 4; }
    Mod+Shift+5 { move-window-to-workspace 5; }
    Mod+Shift+6 { move-window-to-workspace 6; }
    Mod+Shift+7 { move-window-to-workspace 7; }
    Mod+Shift+8 { move-window-to-workspace 8; }
    Mod+Shift+9 { move-window-to-workspace 9; }
    Mod+Shift+0 { move-window-to-workspace 10; }

    // Workspace scroll uses native Niri behavior only.
    Mod+Period { focus-workspace-down; }
    Mod+Comma { focus-workspace-up; }
    Mod+WheelScrollDown cooldown-ms=150 { focus-workspace-down; }
    Mod+WheelScrollUp cooldown-ms=150 { focus-workspace-up; }
    Mod+Shift+WheelScrollDown { focus-column-right; }
    Mod+Shift+WheelScrollUp { focus-column-left; }
    Mod+Shift+Ctrl+WheelScrollDown { move-column-right; }
    Mod+Shift+Ctrl+WheelScrollUp { move-column-left; }
    Mod+Ctrl+WheelScrollDown cooldown-ms=150 { move-window-to-workspace-down; }
    Mod+Ctrl+WheelScrollUp cooldown-ms=150 { move-window-to-workspace-up; }

    // Screenshots.
    Print { screenshot; }
    Shift+Print { screenshot-screen; }
    Ctrl+Print { screenshot; }
    Ctrl+Shift+Print { screenshot-screen; }

    // Session controls and shortcut inhibition toggle.
    Mod+Ctrl+M { quit; }
    Mod+Shift+M { spawn-sh "loginctl terminate-user ''"; }
    Mod+Escape allow-inhibiting=false { toggle-keyboard-shortcuts-inhibit; }
}
