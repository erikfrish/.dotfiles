[Service]
# Regenerate /etc/fancontrol with the current hwmon path before starting.
ExecStartPre=/usr/local/libexec/dotfiles-fancontrol
# The generator writes /etc/fancontrol; disable ProtectSystem=full which would
# make /etc read-only for the ExecStartPre process.
ProtectSystem=false
