# Machine/company-specific SSH config lives in ~/.ssh/config.local.
# This file is an example only; copy it to ~/.ssh/config.local and adjust locally.

# Include ~/.ssh/wb/config

# Host work-gitlab
# 	HostName gitlab.wildberries.ru
# 	User git
# 	IdentityFile ~/.ssh/id_ed25519_sk_rk_erikfrish@yubik_5c_nfc
